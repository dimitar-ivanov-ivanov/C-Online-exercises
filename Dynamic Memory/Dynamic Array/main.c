#include <stdio.h>
#include <stdlib.h>

int main() {
//1 insert a book with y pages at the end of the xth shelf
//2 print the number of pages in yth book of th xth shelf
//3 print the number of books on th xth shelf

    int total_number_of_shelves,
        total_number_of_queries,
        * total_number_of_books,
        ** total_number_of_pages,
        i,command,y,x;

    scanf("%d\n%d",total_number_of_shelves,total_number_of_queries);

    for(i = 0; i < n; i++) {
        scanf("%d",&command);

        switch(command) {
        case 0:
            break;
        default:
            break;
        }
    }



    return 0;
}
