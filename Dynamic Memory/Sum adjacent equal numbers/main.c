#include <stdio.h>
#include <stdlib.h>

#define LEN 100
#define NUMLEN 5

void parseInput(char *str,int *p);

int main() {

    char input[LEN];
    int *p = calloc(1,sizeof(int));
    gets(input);


    return 0;
}

void parseInput(char *str,int *p) {

    char num[NUMLEN];


}
